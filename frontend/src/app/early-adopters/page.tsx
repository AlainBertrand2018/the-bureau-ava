import PlaceholderPage from "@/components/shared/PlaceholderPage";

import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'THE BUREAU | Early Adopters Onboarding',
    description: 'The executive pathway to deploying the AVA system. Apply for early onboarding into the world\'s first autonomous validation environment.',
};

export default function EarlyAdoptersPage() {
    return <PlaceholderPage title="Early Adopters" />;
}
